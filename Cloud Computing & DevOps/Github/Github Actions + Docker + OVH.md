# Usefull Commands

1. Add linux user to docker group
```bash
# add user to docker group
sudo usermod -aG docker ubuntu
```

2. Generate SSH key 
```bash
# generate ssh key
ssh-keygen -t ed25519 -C "your_email@example.com"

# show ssh key
cat ~/.ssh/id_ed25519.pub

#add ssh to authorized keys
```

